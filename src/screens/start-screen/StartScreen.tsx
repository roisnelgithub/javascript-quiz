import { Button } from "@mui/material";

const StartScreen = () => {
  return (
    <>
      <Button variant="contained" sx={{ maxWidth: "120px" }}>
        ¡Comenzar!
      </Button>
    </>
  );
};

export default StartScreen;
